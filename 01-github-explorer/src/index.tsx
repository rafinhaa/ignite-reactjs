import {render} from 'react-dom'; //renderiza o componente
import { App } from './App'; //importa o componente App
//O primeiro parametro é o componente que será renderizado, o segundo é onde ele será renderizado
render(<App/>, document.getElementById('root')); //renderiza o componente dentro da div com id root	
