version: "2"
services:
  node:
    image: "node"
    user: "node"
    working_dir: /srv/meu-app
    environment:
      - NODE_ENV=development
    volumes:
      - ./02-dtmoney:/srv/meu-app
    ports:
      - 3000:3000
    command: "yarn start"
